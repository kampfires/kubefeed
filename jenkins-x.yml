buildPack: git
pipelineConfig:
  pipelines:
    pullRequest:
      preBuild:
        steps:
        - command: git branch
      build:
        replace: true
        steps:
        - command: echo noodle
